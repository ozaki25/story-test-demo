import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="解説/0. このリポジトリについて" />

# Story を使ったコンポーネントテストの比較

React コンポーネントに Story を書き、その Story を土台にした複数のテスト手法を
同じ題材で書き比べるためのリポジトリです。技術選定の材料にすることを目的にしています。

「動くサンプル」ではなく「比べられるサンプル」を目指しているので、
どの手法でも**同じシナリオ**を実装しています。

## 比較の土台になるケース

「書き方が違う」以上のことを言うために、`LoginForm` に対する検証ケースを固定し、
どの手法でも同じ内容を実装しています。

ケースは思いつきではなく技法から導出しています。
状態遷移テストで T1〜T6、デシジョンテーブルで D1〜D4、
同値分割・境界値分析で V1〜V8、操作経路で K1。
導出の過程と、どのケースをどの手法に置くかの判断は「解説 / 8. テスト設計」にあります。

ケースを洗い出す過程で、実装の欠陥が 2 件見つかりました。
送信失敗時に未処理の Promise 拒否になっていたことと、
Enter キーによる二重送信を防いでいなかったことです。

## 収録している手法

| #      | 手法                      | 実行環境   | 書く場所                               |
| ------ | ------------------------- | ---------- | -------------------------------------- |
| ①      | `play` 関数               | 実ブラウザ | `*.stories.tsx`                        |
| ②      | 全 Story のスモークテスト | 実ブラウザ | 書かなくてよい（Story を書けば増える） |
| ③      | a11y（axe）               | 実ブラウザ | `parameters.a11y`                      |
| ④      | Portable Stories          | jsdom      | `*.portable.test.tsx`                  |
| 対照群 | 素の Testing Library      | jsdom      | `*.plain.test.tsx`                     |

2026 年 8 月時点で、本番で採用しうるものだけに絞っています。
Storybook 8 で削除された storyshots、更新の止まっている vitest-axe、
Vite 構成では出番のない test-runner は入れていません。

## このドキュメントの書き方

解説には、このリポジトリで実際に観測できたことと、それを再現する方法だけを書いています。

- 実行時間・カバレッジ・エラーメッセージ・バージョンは、すべて手元で計測した値です
- 「こう書くべき」「こうしたほうがよい」といった一般論は書きません
- 方式の優劣は断定せず、観測された差だけを並べます。選ぶのは読み手です

## どこに何が書いてあるか

| 読みたいもの                     | ページ            |
| -------------------------------- | ----------------- |
| 各手法の書き方と向き不向き       | 1〜4              |
| 実装中に踏んだ落とし穴と再現条件 | 5. 落とし穴       |
| 手法の比較、使い分け、実測値     | 6. 比較と使い分け |
| ディレクトリ構成と設計判断       | 7. 構成と設計判断 |
| ケースの導出と手法への割り当て   | 8. テスト設計     |

## テストの実行

```bash
npm run test           # 全部
npm run test:stories   # 実ブラウザ（①②③）
npm run test:unit      # jsdom（④と対照群）
npm run test:portable  # 手法④だけ
npm run test:plain     # 対照群だけ
```

Vitest の project を実行環境で分けているので、片方だけを回せます。
CI での並列化や部分実行がしやすくなります。

手法単位の実行は、収集条件ではなく実行時のファイル名で絞っています。
`include` を手法別サフィックスで絞ると、規約から外れた名前のテストが
警告もなく無視されるためです（「解説 / 5. 落とし穴」を参照）。
